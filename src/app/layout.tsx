import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NearByBites 🍽️ - Discover Amazing Food Around You",
  description: "AI-powered restaurant discovery with personalized food recommendations, nearby restaurants, trending dishes, and smart food suggestions.",
  keywords: ["restaurant discovery", "AI food finder", "nearby food", "trending dishes", "food recommendations", "NearByBites"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
